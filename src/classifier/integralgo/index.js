"use strict";

/** avgEpochs calculates avg sample values for each channel (sum of ch from all epochs for given key divided by number
 *  of epochs for given key)
 * of that key epochs)
 *
 * @param {Array} epochs
 * @return {Array}
 */
function avgEpochs(epochs) {

}
